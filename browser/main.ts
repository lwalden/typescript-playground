// browser/main.ts
// Edit this file to see output in the browser (hot reload via Vite).
// Run: npm run browser  → open http://localhost:5173

function greet(name: string): string {
  return `Hello, ${name}!`;
}

console.log(greet("Playground"));
console.log("Edit browser/main.ts and save — the page updates instantly.");
